query UserPlaylists($input: SearchUserPlaylistsInput!) {
   user {
      id
      playlists(input: $input) {
         id
         images
         name
         owner {
            ...UserWithSpotifyOverview
         }
      }
   }
}
