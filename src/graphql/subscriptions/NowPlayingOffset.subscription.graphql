subscription NowPlayingOffset($input: Int!) {
    nowPlaying(tickInterval: $input) { 
        timestamp
        progressMs
        isPlaying
        shuffleState
        item {
            id
            durationMs
            name
            artists {
                id
                name
            }
            album {
                name 
                images
            }
        }
    }
}
