query AvailableDevices {
  availableDevices {
    ...PlaybackDevice
  }
}

fragment PlaybackDevice on PlaybackDevice {
    id
    isActive
    isPrivateSession
    isRestricted
    name
    type
    volumePercent
}