fragment DetailedComment on Comment {
   id
   commentIndex
   reviewId
   createdAt
   updatedAt
   deleted
   parentCommentId
   commenter {
      ...UserWithSpotifyOverview
   }
   comment
   entities {
      ...ReviewEntityOverview
   }
}
